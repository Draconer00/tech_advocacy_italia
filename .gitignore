.venv/
.env

# Claude Code local configuration — agents, commands, memory, worktrees
.claude/
CLAUDE.md
agents.md

# Dati raccolti dagli scraper (raw e processati)
data/raw/
data/processed/*
data/*.db*
data/snapshots/

# Eccezione: le correzioni umane della dashboard (Gold Standard per l'active
# learning) sono poche righe di label curate, non dati grezzi scraped — le
# versioniamo così la pipeline CI può riaddestrare il modello di urgenza.
!data/processed/training_data_feedback.csv

# Cache e modelli
*.pkl
*.joblib
*.model

# API keys e configurazioni locali
*.env.*
secrets.py

# File temporanei dashboard
network_graph*.html

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
